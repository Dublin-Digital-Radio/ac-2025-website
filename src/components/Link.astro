---
import type { HTMLAttributes } from "astro/types";

interface Props extends Omit<HTMLAttributes<"a">, "href"> {
  href: string;
}

const { href, ...rest } = Astro.props;
---

<a
  href={href.includes("://") ? href : `${import.meta.env.BASE_URL}${href}`}
  {...rest}><slot /></a
>
