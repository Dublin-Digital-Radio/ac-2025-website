---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href, ...rest } = Astro.props;
---

<a href={`${import.meta.env.BASE_URL}${href}`} {...rest}><slot /></a>
